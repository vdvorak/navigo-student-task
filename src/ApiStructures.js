export const idParam = {
    _className: 'IdParam',
    // Input params
    id: true, // boolean value sais if the param is required or not
  }
  
  export const userData = {
    _className: 'UserData',
    // Input params
    id: false, // boolean value sais if the param is required or not
    name: true,
    surname: true,
    hourlyRate: true,
  }
  
  export const usersFilter = {
    _className: 'UsersFilter',
    // Input params
  }
  